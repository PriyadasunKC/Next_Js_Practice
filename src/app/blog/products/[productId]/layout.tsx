export default function ProductdetaislLayout({ children }: {
    children: React.ReactNode;
}) {
    return (
        <>
            {children}
            <h2>Feature Products</h2>
        </>
    )
}